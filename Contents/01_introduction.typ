#import "../template.typ": *

= 项目简介与基础排版 <sec-intro>

这是一个使用 *Typst* 编写并转换至高保真 *EPUB 3 / 2* 电子书的示例文档。

== 核心设计目标

本项目致力于解决从学术/文档排版系统向现代电子书阅读器交付时的格式保真度问题：

- *分章与结构化导航*：自动以一级标题（`=`）作为章节切分边界，同时向下递归提取二级（`==`）、三级（`===`）标题构建多级嵌套目录树（TOC）。
- *分章独立脚注*：自动将全局分散的脚注重构并精准挂载至其所在章节末尾，构建双向无缝跳转锚点#footnote[这是一个第一章内的示例脚注，用于测试当章末尾回跳功能。]。
- *深色模式全适配*：内嵌样式表天然支持 `@media (prefers-color-scheme: dark)`，在微信读书、Apple Books、Kobo 等各类阅读器深色主题下保持文字与卡片的可读性。

== 基础文本样式展示

本转换器完整继承 Typst 的丰富行内排版语法：

- *粗体强调*（Bold）与 _斜体文字_（Italic）
- 带有 `行内代码`（Inline Code）与删除线展示
- 中英文混排时的空隙自动优化及标点悬挂
- 支持多段落自然首行缩进与段落行高优化

在文本中我们还可以添加第二个脚注#footnote[第二个脚注测试：验证多脚注在同一章节的列表排列。]，电子书阅读器将能顺畅解析并在页面弹出或跳转。
